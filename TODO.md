# TODO — Текущее состояние и известные ограничения

Актуальный список нереализованных возможностей, ограничений и задач проекта UltraDecompiler.

**Цель проекта:** генерировать C-код, максимально близкий по стилю и структуре к Microsoft QuickC Compiler 1.0.

---

## Правила ведения этого файла

- При **закрытии**, частичном решении или значительном прогрессе по любому пункту — обязательно обновляй этот файл (`TODO.md`).
- Если изменение затрагивает архитектуру, принципы работы или описание компонентов — также обновляй [AGENTS.md](./AGENTS.md).
- При добавлении новых ограничений или задач добавляй их сюда с достаточным описанием.
- Можно добавлять пометки о статусе, например: `(в процессе)`, `(частично реализовано)`, `(блокировано ...)`.

---

## Текущие ограничения и задачи

### 1. Инструкции дизассемблера / ExpressionBuilder

**Не реализованы или частично:**
- `MUL`, `IMUL`, `DIV`, `IDIV`
- `DAA/DAS/AAA/AAS`, `PUSHF/POPF/LAHF/SAHF`, `RCL/RCR` (ротации с CF)
- PUSH/POP с непосредственным операндом (68h/6Ah), POP в память через 8F
- `CWD` (`CBW` добавлен)
- `ENTER` (level > 0) — базовый ENTER (level=0) добавлен

**Строковые операции** — значительный прогресс (большая часть реализована):
- Одиночные MOVS, STOS, LODS, CMPS, SCAS — полностью реализованы (разложены на примитивы IR: `StoreOperation`, `SetOperation`, `CmpExpr`).
- `CLD`/`STD` и флаг DF — полноценно поддержаны на уровне `RegisterExpressions` и `ExpressionBuilder`.
- REP MOVS / STOS / LODS — реализованы через `WhileOperation` (с умным пост-состоянием регистров).
- REPZ / REPNZ CMPS и SCAS — реализована поддержка циклов с составными условиями выхода (`CX != 0 && ZF == expected`).
- Обработка случая `CX = 0` перед REP (цикл не создаёт) — сделана.
- Добавлены из категории «легко»: ENTER, ROL, ROR, LOOP/LOOPE/LOOPNE, IN/OUT (2026).
- Добавлены «простые» инструкции: XCHG (полностью), LEAVE, CBW, ADC, SBB (2026).
- Обновление регистров после REP-циклов (точные значения при известном CX, post-loop переменные иначе) — работает.
- Покрытие тестами новой логики — хорошее (ключевые методы на 100% по строкам).

**Оставшиеся задачи по строковым операциям:**
- Полировка обновления флагов после REPZ/REPNZ CMPS/SCAS.
- Полная поддержка сегментных префиксов внутри REP-инструкций.
- Генерация красивого C-кода (Этап 9): far-указатели, идиоматические циклы, автоматическое распознавание `memset`/`_fmemset`/`memcpy`.

---

### 2. LibParser и LibMatching (новое, 2026)

**Реализовано:**
- Парсер OMF `.LIB` QuickC: модули, LEDATA/LIDATA, EXTDEF, FIXUPP, словарь символов (`LibParser`).
- `OmfRelocationTableBuilder` — таблица релокаций для дизассемблирования кода модуля.
- `OmfFixupNameResolver` — имена целей FIXUPP.
- `LibraryFunctionMatcher` + `FunctionBodyComparer` — сопоставление тела функции EXE с символом `.LIB` (с маскированием rel16 и near CALL/JMP).
- `Crt0EntryPointMatcher` — выбор библиотеки по совпадению crt0/`__astart` на точке входа.
- `MainOffsetFinder` — поиск `_main` по FIXUPP вызова из crt0.
- CLI `decompile-main`: crt0 → `_main` → `DecompilePipeline`.
- CLI `lib -s SYMBOL`: разбор модуля, таблица FIXUPP, дизассемблирование CODE.
- Тесты: `LibParserTests`, `LibMatchingTests` (эталонные `.LIB` / `.EXE` из `QuickC/`).

**Ограничения и задачи:**
- Нет разбора PUBDEF — точка входа символа в CODE предполагается offset 0 (типично для однофайловых модулей QuickC, но не всегда).
- `FunctionBodyComparer`: тело в EXE может быть длиннее тела в `.LIB` (границы функции не определяются точно; CALL без RET).
- `LibrarySymbolFinder` — линейный перебор всего образа (медленно для больших EXE).
- Сопоставление только по совпадению инструкций; нет call graph и именования произвольных `sub_XXXX` через runtime.
- Некоторые модули `.LIB` (напр. CLIBFP) не дизассемблируются полностью — такие символы пропускаются.
- Расширить сопоставление на runtime-функции (`_printf`, `_malloc` и т.д.) для восстановления имён и типов.

---

### 3. Анализ процедур

- **Реализовано (2026):** восстановление **входных параметров** декомпилируемой функции в `ExpressionBuilder` — пролог `push bp; mov bp, sp` / `ENTER`, обращения `[BP+offset]` (offset ≥ 4) → переменные `arg0`, `arg1`, …; чтения параметров в IR подменяются на `Variable` вместо `MemExpr`.
- **Реализовано (2026):** сигнатуры в `DisassembledProcedure.Signature` — библиотечные функции из `QuickC/INCLUDE` (`QuickCHeaderCatalog`), пользовательские из `ProcedureSignatureAnalyzer` (пролог + `[BP+n]`, эвристика `void`/`int` по записи в AX). `CallHandler` берёт callee из `ProcedureStorage`, аргументы — со стека (cdecl) / PUSH перед CALL (variadic). `CallExpr(string Name, …)` вместо класса `Procedure`.
- Нет анализа эпилога, локальных переменных `[BP-offset]`, точных типов параметров пользовательских функций (пока `int`).
- Косвенные вызовы и far call — без сигнатуры; только адрес в аргументах.
- LibMatching даёт имена только для символов, чьё тело совпало с кодом в образе; произвольные процедуры пользователя не восстанавливаются.
- Отсутствует построение графа вызовов (call graph).

---

### 4. Моделирование памяти

- Базовое (через `MemExpr` + `StoreOperation`).
- Есть распознавание PSP, но полноценного анализа стека/кучи/глобальных данных нет.
- TODO: объединить Address + Segment в `StoreOperation` (`StoreOperation.cs`).

---

### 5. Флаги

- PF (чётность) не отслеживается.
- Часть флаговой арифметики — приближённая.
- `CLI`/`STI` → `CallOperation` `_disable()` / `_enable()` (ExpressionBuilder + msdos.h).
- `CLD`/`STD` → обновление DF; `CLC`/`STC`/`CMC` → CF. См. `FlagHandler`, `FlagModelingTests`.

---

### 6. Code Generation

- ExpressionBuilder строит IR (`ExprBlock` + `Operations`, включая `WhileOperation`/`ForOperation`).
- Генерация финального C-кода **ещё не реализована** (планируется в Этапе 9).
- Отсутствует pretty-printing выражений обратно в синтаксис C.
- Цель — генерация кода в стиле QuickC 1.0 (far-указатели, идиоматические циклы, распознавание `memset` и т.д.).

---

### 7. Прочее

- Обработка `CALL` в CFG помечена TODO (нужна проверка «есть ли return» и построение процедур; `ControlFlowGraph.cs`).
- Переходы в середину инструкции — исключение.
- Отсутствует анализ и восстановление типов (всё пока 16-битные целые + указатели).
- ExpressionBuilder: слияние состояния при входе в блок из нескольких предшественников — не решено (`ExpressionBuilder.cs`).

---

## Пожелания и направления развития (не в приоритете)

- Поддержка генерации структур и union'ов на основе анализа обращений к памяти
- Восстановление имён переменных и функций (где возможно) — частично через LibMatching
- Экспорт CFG и IR в удобные для анализа форматы
- Поддержка большего количества DOS-вызовов через `msdos.h`
- Интеграция с дизассемблерами (IDA/Ghidra) для улучшения качества входных данных

---
